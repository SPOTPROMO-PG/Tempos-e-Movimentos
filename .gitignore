# Planilhas fonte com dados internos (CNPJ, endereços, IDs de loja/promotor).
# Não devem ir para o repositório público — servem só de referência local.
*.xlsx

# Config local do Claude Code
.claude/

# Pacote Android gerado (contém a chave de assinatura privada — nunca
# deve ir para um repositório público). Guarde isso em local seguro,
# fora do git; é necessário para publicar futuras atualizações do app
# com a mesma identidade.
apk-build*/

# Pacote de lançamento (APK + materiais de apoio) — gerado, fica local.
lancamento/

# artefatos de análise (dados exportados e cache do Python)
analise/__pycache__/
analise/eventos.csv
analise/saida/_fig/

# Artefatos de outras ferramentas que caíram na pasta de trabalho
work_auditoria/
*.inspect.ndjson
mso*.tmp
